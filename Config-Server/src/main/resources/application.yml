server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Java-Techie-jt/insurance-config-server
          default-label: master
#to read properties: http://localhost:8888/application/default